---
import Layout from '../layouts/Layout.astro';
import { nouns } from '../lib/nouns.ts';

const count = nouns.length;
const combinations = count * (count - 1);
---

<Layout title="Statistikk">
  <div id="statistics">
    <h1>Statistikk:</h1>
    <table>
      <tr>
        <th>Antall ord:</th>
        <td>{count}</td>
      </tr>
      <tr>
        <th>Antall mulige kombinasjoner:</th>
        <td>{combinations}</td>
      </tr>
    </table>
  </div>
</Layout>